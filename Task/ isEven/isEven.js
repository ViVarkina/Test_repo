// 11

function isEven(number) {
    if (typeof number !== "number") {
        return("Неправильный тип переменной")
    }
    else if (number / 2 ) {
        return ("true")
    }
    else {
        return ("false")
    }
}

// не решен